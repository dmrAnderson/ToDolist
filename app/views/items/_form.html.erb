<%= simple_form_for(@create_item, html: { class: "border rounded bg-white px-3 py-3"}) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.button :submit, "Create", class: "btn btn-light btn-lg btn-block" %>
<% end %>
